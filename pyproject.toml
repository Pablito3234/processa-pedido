[project]
name = "processador-pedidos"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "django>=5.0.6",
    "djangorestframework>=3.15.2",
    "pika>=1.3.2",
    "psycopg2-binary>=2.9.9",
    "gunicorn>=22.0.0",
]

[tool.uv]
dev-dependencies = []
